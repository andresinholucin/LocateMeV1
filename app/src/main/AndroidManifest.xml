<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ec.edu.upse.locatemev1">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".configuracion.VariablesGenerales"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controladores.principal.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controladores.tabsControl.MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".controladores.ubicacionUsuarioControl.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".controladores.usuarioTutoriadoControl.nombreapellido" />
        <activity android:name=".controladores.usuarioTutoriadoControl.datospersonales1" />
        <activity android:name=".controladores.usuarioTutoriadoControl.datospersonales2" />
        <activity android:name=".controladores.usuarioTutoriadoControl.configuracionUsuario" />
        <activity android:name=".controladores.usuarioTutoriadoControl.usuariocontrasenia" />
        <activity android:name=".controladores.usuarioTutoriadoControl.perfilUsuarioTutoreado" />
        <activity android:name=".controladores.principal.MainTutoreadoActivity"></activity>
        <activity android:name=".controladores.usuarioTutorControl.PerfilUsuarioTutor" />
        <activity android:name=".controladores.usuarioTutorControl.RegistrarDatosTutor" />
        <activity android:name=".controladores.usuarioTutorControl.RegistrarDatosTutor2" />
        <activity android:name=".controladores.usuarioTutorControl.RegistrarDatosTutor3" />
        <activity android:name=".controladores.usuarioTutorControl.RegistrarDatosTutor4" />


    </application>

</manifest>